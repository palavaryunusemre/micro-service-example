server:
  port: 8888

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/palavaryunusemre/microservice-config-service.git"
          default-label: "main"
          skip-ssl-validation: true
          username: "yunusemrepalavar"
          password: ${GIT_KEY}